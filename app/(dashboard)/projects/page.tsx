"use client"
import Link from "next/link"
import { Plus, ArrowUpRight } from "lucide-react"
import { useDashboard } from "@/components/dashboard/store"
import { Card, DemoBadge, PageHeader, StatusBadge } from "@/components/dashboard/ui"
export default function ProjectsPage(){const {projects}=useDashboard();return <div className="space-y-6"><PageHeader title="Projects" subtitle="Manage environmental projects and their monitored sites."><DemoBadge/><Link href="/projects/new" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground"><Plus className="size-4"/> New Project</Link></PageHeader><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.map(p=><Card key={p.id} className="p-5 transition-colors hover:border-primary/40"><div className="flex items-start justify-between gap-3"><div><div className="text-xs uppercase tracking-wider text-muted-foreground">{p.project_type}</div><h2 className="mt-2 font-heading text-lg font-semibold">{p.name}</h2></div><StatusBadge status={p.status}/></div><p className="mt-3 line-clamp-2 text-sm text-muted-foreground">{p.description}</p><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><div className="text-xs text-muted-foreground">Region</div><div className="mt-1 font-medium">{p.region}</div></div><div><div className="text-xs text-muted-foreground">Status</div><div className="mt-1 font-medium">{p.status}</div></div></div><Link href={`/projects/${p.id}`} className="mt-5 inline-flex items-center gap-1 text-sm text-primary">View project <ArrowUpRight className="size-4"/></Link></Card>)}</div></div>}
